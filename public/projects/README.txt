Drop project screenshots here, referenced from src/data/profile.ts:

  - echomeet.png   (EchoMeet screenshot — referenced as project.image)
  - wanderlust.png (WanderLust screenshot — referenced as project.image)

Recommended size: 1200x800px, optimized (use https://squoosh.app or `npx @squoosh/cli`).
If a screenshot isn't present yet, the project card currently uses a placeholder
gradient block — see ProjectCard.tsx for where the <img> would be added once you
have real screenshots.
